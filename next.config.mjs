/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // Already default in latest Next.js
  // Add any other Next.js specific configurations here
};

export default nextConfig; 